[project]
  # Your LiveKit Cloud project subdomain — found in LiveKit Cloud → Settings (e.g. "my-project-abc123")
  subdomain = ""

[agent]
  # Filled in automatically after `lk agent create`, or copy from `lk agent list`
  id = ""
